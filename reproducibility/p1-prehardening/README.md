# P1 pre-hardening runtime snapshot

This directory preserves the exact `minimal_api.py` implementation used for the reported pre-hardening P1 corruption run (24/45 detected; EVDR=0.5333).

It is intentionally stored separately from the hardened `src/minimal_api.py`. The same frozen 45-case manifest is used for both states, so the comparison does not depend on reconstructing private development history.
